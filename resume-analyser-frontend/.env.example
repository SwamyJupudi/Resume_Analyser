# Copy this file to .env and adjust if your backend runs elsewhere.
# The backend's own application.properties sets server.port=8081,
# so that is the correct default despite common Spring Boot convention.
VITE_API_BASE_URL=http://localhost:8081
